package kotlin14
/*public -> modificador de acceso predeterminado
* permite que las declaraciones sea accesible en todas partes
* private -> solo permite se accesible en la misma clase
* protected -> permite que la declaraciones sean accesicles
* Internal -> permite que la delcaración sea accesible
* en le mismo modulo similar a private puedes acceder a propiedas y
* métodos internos desde fuera de la clase siempre y cuando lo
* hagas en el mismo módulo*/
fun main() {


}